# Godot
.godot/
*.translation
.import/

# Android build outputs (the installed template under android/build is committed;
# its build products, the godot-lib AARs and per-export generated assets are not —
# run tools/setup_android_template.sh after cloning to restore libs/)
android/build/build/
android/build/.gradle/
android/build/libs/
android/build/assets/
build/
*.iml
.gradle/
local.properties

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
